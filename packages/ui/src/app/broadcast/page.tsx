import ContactCollection from "./_contactCollection";

const Page = () => {
  return (
    <section className="flex flex-row w-full">
      <ContactCollection />
    </section>
  );
};

export default Page;
